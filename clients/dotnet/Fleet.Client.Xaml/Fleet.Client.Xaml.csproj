<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Fleet.Client.Core\Fleet.Client.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.2" />
  </ItemGroup>

  <ItemGroup>
    <!-- The sparkline arithmetic is internal because no host should call it directly, but it
         is the one piece of this assembly with enough arithmetic to be worth testing on its
         own rather than through a ViewModel. -->
    <InternalsVisibleTo Include="Fleet.Client.Xaml.Tests" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
